Projects on GitHub

1. Data Bases:
   1.1. SELECT function;
   1.2. JOIN function.

2. Aplications:
   2.1. Page with cats maked on html;
   2.2. Page with cats maked on html + css;
   2.3. Calculators (adding, substruction, multiplying) maked on html + css + js;
   2.4. Calculator All_In_One maked on html + css + js.

3. Automated testing (Postman collection).
   
